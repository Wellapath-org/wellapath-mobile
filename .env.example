API_BASE_URL=http://192.168.1.148:3000
ARTIFACT_BASE_URL=https://d179u2ex0g66o3.cloudfront.net
APP_ENV=staging
ENABLE_OFFLINE_MODE=true
API_TIMEOUT_MS=10000

# --- Telemetry (I1/W1) -------------------------------------------------------
# Privacy-safe product analytics, contract v1.0. Off unless explicitly enabled.
TELEMETRY_ENABLED=false
# Optional; falls back to API_BASE_URL.
TELEMETRY_BASE_URL=
# Production is disabled even with TELEMETRY_ENABLED=true unless this is also
# set. Requires founder + engineering lead sign-off.
TELEMETRY_PRODUCTION_APPROVED=false
